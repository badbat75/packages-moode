# libopenaptx_0.2.0+5eb14ed
PKG_URL="https://github.com/pali/libopenaptx/archive/5eb14ed25a73ebbc3c086f0807f379339e628146.tar.gz"
#PATCHES=
BUILD_PROCESS=simplemake
#INST_CMD=install
PKG_DEPS=
PKG_MAKEVARS="PREFIX=${BUILD_PREFIX} LIBDIR=lib${BUILD_LIBSUFFIX}"
PKG_PREBUILD="
    sed -i 's/CFLAGS = /CFLAGS ?= /g' Makefile
    sed -i 's/PREFIX = /PREFIX ?= /g' Makefile
    sed -i 's/BINDIR = /BINDIR ?= /g' Makefile
    sed -i 's/LIBDIR = /LIBDIR ?= /g' Makefile"
