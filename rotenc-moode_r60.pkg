# rotenc-moode_r60
PKG_SRCDIR=moode_r60
BUILD_PROCESS=simplemake
INST_CMD=install
PKG_DEPS="moode/moode_r60 moode/wiringpi-master"
PKG_MAKEVARS="PREFIX=\${BUILD_PREFIX} -C other/rotenc"
PKG_POSTBUILD="install -d ${BIN_PATH}/usr/local/bin &&
ln -fs /usr/bin/rotenc ${BIN_PATH}/usr/local/bin/rotenc"
